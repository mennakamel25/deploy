---
apiVersion: v1
kind: Service

metadata:
  name: jenkins-app-service

spec:
  type: NodePort

  selector:
    app: jenkins-app

  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30007
